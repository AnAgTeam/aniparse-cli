// Generated by CMake from cmake/anip_extensions.hpp.in — DO NOT EDIT.
//
// Seam A (static extension composition): extra parser registrars linked into
// THIS anip build. Empty by default, so the public binary ships only the
// showcase set. Populate via the CMake cache variables (see CMakeLists.txt):
//   ANIP_EXTENSION_SUBDIRS    dirs to add_subdirectory (bring targets into scope)
//   ANIP_EXTENSION_LIBS       targets to link into anip
//   ANIP_EXTENSION_HEADERS    headers declaring the registrar functions
//   ANIP_EXTENSION_REGISTRARS registrar functions, each void(aniparse::ParserStore::Edit&)
//   ANIP_EXTENSION_EXTRACTOR_REGISTRARS registrar functions, each void(aniparse::VideoExtractorStore::Edit&)
#pragma once
#include <aniparse/ParserStore.hpp>
#include <aniparse/video/VideoExtractor.hpp>
@ANIP_EXTENSION_INCLUDES@
namespace aniparse::cli {

/// Add every extension parser set linked into this build to an already-open
/// store edit, after the default showcase set. The caller commits the edit, so
/// the whole registration publishes one routing snapshot. A no-op when no
/// extensions were configured.
inline void register_extensions([[maybe_unused]] aniparse::ParserStore::Edit& edit) {
@ANIP_EXTENSION_CALLS@}

/// Add every extension video-extractor set linked into this build to an
/// already-open store edit, without committing. A no-op when no extractor
/// registrars were configured.
inline void register_extension_extractors([[maybe_unused]] aniparse::VideoExtractorStore::Edit& edit) {
@ANIP_EXTENSION_EXTRACTOR_CALLS@}

} // namespace aniparse::cli
